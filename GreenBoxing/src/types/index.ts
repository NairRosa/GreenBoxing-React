export interface App {
  id: number;
  title: string;
  description: string;
  imageUrl: string;
  downloadUrl: string;
}

export interface HeroContent {
  title: string;
  subtitle: string;
}

export interface SocialLinks {
  instagram: string;
  github: string;
  phone: string;
  email: string;
}